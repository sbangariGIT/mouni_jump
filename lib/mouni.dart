import 'package:flutter/material.dart';

class Mouni extends StatelessWidget {
  const Mouni({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
        height: 100, width: 100, child: Image.asset("lib/assets/mouni.png"));
  }
}
